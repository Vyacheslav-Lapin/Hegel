package com.hegel.core;

public interface Streams {

//    static <T> boolean equals(Stream<T> stream1, Stream<T> stream2) {
//        assert stream1.isParallel() && stream2.isParallel();
//        return
//    }

//    static <T> boolean equalsElements(Stream<T> stream, T stream)
}
